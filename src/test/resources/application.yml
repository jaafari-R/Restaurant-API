spring:
  application:
    name: temporal-demo-test
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: bisbis10
    password: bisbis10
    driverClassName: org.h2.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always